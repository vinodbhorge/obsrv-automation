namespace: superset
# global:
global:
  postgresql:
    user: "postgres"
    password: "postgres"
    host: "postgresql-hl.postgresql.svc.cluster.local"
    port: 5432
  redis_dedup:
    host: valkey-dedup-headless.redis.svc.cluster.local
    port: 6379

postgresql:
  db_name: "superset"
redis:
  db_index: 3

replicaCount: 1
oauth_enabled: true

adminUser:
  username: "admin"
  firstname: "Superset"
  lastname: "Admin"
  email: "admin@superset.com"
  password: "admin123"

oauth:
  enabled: false
  client_id: ""
  client_secret: ""
  auth_token: ""
  email_whitelist_regex: ""
  whitelist_domain: ""
  user_registration_role: "Admin"

serviceAccountName: "superset-sa"

runAsUser: 1000

# Install additional packages and do any other bootstrap configuration in this script
# For production clusters it's recommended to build own image with this step done in CI
bootstrapScript: |
  #!/bin/bash
  #rm -rf /var/lib/apt/lists/* && \
  if [ ! -f ~/bootstrap ]; then echo "Running Superset with uid {{ .Values.runAsUser }}" > ~/bootstrap; fi

## The name of the secret which we will use to generate a superset_config.py file
## Note: this secret must have the key superset_config.py in it and can include other files as well
##
configFromSecret: '{{ template "superset.fullname" . }}-config'

## The name of the secret which we will use to populate env vars in deployed pods
## This can be useful for secret keys, etc.
##
envFromSecret: '{{ template "superset.fullname" . }}-env'
## This can be a list of template strings
envFromSecrets: []

## Extra environment variables that will be passed into pods
##
extraEnv:
  PREFERRED_URL_SCHEME: "https"
  COGNITO_ALLOWED_GROUP: "us-east-1_QuCefvh0J_superset-users"
  # Extend timeout to allow long running queries.
  # GUNICORN_TIMEOUT: 300


   # OAUTH_HOME_DOMAIN: ..
  # # If a whitelist is not set, any address that can use your OAuth2 endpoint will be able to login.
  # #   this includes any random Gmail address if your OAuth2 Web App is set to External.
  # OAUTH_WHITELIST_REGEX: ...

## Extra environment variables to pass as secrets
##
extraSecretEnv: {}
  # MAPBOX_API_KEY: ...
  # # Google API Keys: https://console.cloud.google.com/apis/credentials
  # GOOGLE_KEY: ...
  # GOOGLE_SECRET: ...

extraConfigs:
  import_datasources.yaml: |
    databases:
    - allow_csv_upload: true
      allow_ctas: true
      allow_cvas: true
      database_name: obsrv
      allow_dml: true
      sqlalchemy_uri: druid://dataset-api.dataset-api.svc.cluster.local:3000/v2/obsrv/data/sql-query
      uuid: 04e7a613-525a-4ec7-80b6-e703584cc743
      version: 1.0.0
      tables:
      - table_name: system-events
        main_dttm_col: __time
        description: null
        default_endpoint: null
        offset: 0
        cache_timeout: null
        schema: druid
        sql: null
        params: null
        template_params: null
        filter_select_enabled: false
        fetch_values_predicate: null
        extra: null
        uuid: 62edf6c5-87c8-4b03-8a0a-f67613d3e24d
        metrics:
        - metric_name: total_processing_time
          verbose_name: Total Processing Time
          metric_type: double
          expression: SUM(total_processing_time)
          description: null
          d3format: null
          warning_text: null
        - metric_name: latency_time
          verbose_name: Latency Time
          metric_type: double
          expression: SUM(latency_time)
          description: null
          d3format: null
          warning_text: null
        - metric_name: processing_time
          verbose_name: Processing Time
          metric_type: double
          expression: SUM(processing_time)
          description: null
          d3format: null
          warning_text: null
        - metric_name: extractor_time
          verbose_name: Extractor Time
          metric_type: double
          expression: SUM(extractor_time)
          description: null
          d3format: null
          warning_text: null
        - metric_name: validator_time
          verbose_name: Validator Time
          metric_type: double
          expression: SUM(validator_time)
          description: null
          d3format: null
          warning_text: null
        - metric_name: dedup_time
          verbose_name: Deduplication Time
          metric_type: double
          expression: SUM(dedup_time)
          description: null
          d3format: null
          warning_text: null
        - metric_name: denorm_time
          verbose_name: Denormalization Time
          metric_type: double
          expression: SUM(denorm_time)
          description: null
          d3format: null
          warning_text: null
        - metric_name: transformer_time
          verbose_name: Transformer Time
          metric_type: double
          expression: SUM(transformer_time)
          description: null
          d3format: null
          warning_text: null
        - metric_name: error_count
          verbose_name: Error Count
          metric_type: long
          expression: SUM(error_count)
          description: null
          d3format: null
          warning_text: null
        - metric_name: count
          verbose_name: COUNT(*)
          metric_type: count
          expression: COUNT(*)
          description: null
          d3format: null
          warning_text: null
        - metric_name: count
          verbose_name: COUNT(*)
          metric_type: count
          expression: COUNT(*)
          description: Count
          d3format: null
          warning_text: null
        columns:
        - column_name: __time
          verbose_name: Event Time
          is_dttm: true
          is_active: true
          type: LONG
          advanced_data_type: null
          groupby: true
          filterable: true
          expression: null
          description: Event Time
          python_date_format: null
          extra: null
        - column_name: event_type
          verbose_name: Event Type
          is_dttm: false
          is_active: true
          type: STRING
          advanced_data_type: null
          groupby: true
          filterable: true
          expression: null
          description: Event Type
          python_date_format: null
          extra: null
        - column_name: ctx_module
          verbose_name: Context Module
          is_dttm: false
          is_active: true
          type: STRING
          advanced_data_type: null
          groupby: true
          filterable: true
          expression: null
          description: Context Module
          python_date_format: null
          extra: null
        - column_name: ctx_pdata_id
          verbose_name: Context Pdata Id
          is_dttm: false
          is_active: true
          type: STRING
          advanced_data_type: null
          groupby: true
          filterable: true
          expression: null
          description: Context Pdata Id
          python_date_format: null
          extra: null
        - column_name: ctx_pdata_type
          verbose_name: Context Pdata Type
          is_dttm: false
          is_active: true
          type: STRING
          advanced_data_type: null
          groupby: true
          filterable: true
          expression: null
          description: Context Pdata Type
          python_date_format: null
          extra: null
        - column_name: ctx_pdata_pid
          verbose_name: Context Pdata Pid
          is_dttm: false
          is_active: true
          type: STRING
          advanced_data_type: null
          groupby: true
          filterable: true
          expression: null
          description: Context Pdata Pid
          python_date_format: null
          extra: null
        - column_name: error_pdata_id
          verbose_name: Error Pdata Id
          is_dttm: false
          is_active: true
          type: STRING
          advanced_data_type: null
          groupby: true
          filterable: true
          expression: null
          description: Error Pdata Id
          python_date_format: null
          extra: null
        - column_name: error_pdata_status
          verbose_name: Error Pdata Status
          is_dttm: false
          is_active: true
          type: STRING
          advanced_data_type: null
          groupby: true
          filterable: true
          expression: null
          description: Error Pdata Status
          python_date_format: null
          extra: null
        - column_name: error_type
          verbose_name: Error Type
          is_dttm: false
          is_active: true
          type: STRING
          advanced_data_type: null
          groupby: true
          filterable: true
          expression: null
          description: Error Type
          python_date_format: null
          extra: null
        - column_name: error_code
          verbose_name: Error Code
          is_dttm: false
          is_active: true
          type: STRING
          advanced_data_type: null
          groupby: true
          filterable: true
          expression: null
          description: Error Code
          python_date_format: null
          extra: null
        - column_name: error_message
          verbose_name: Error Message
          is_dttm: false
          is_active: true
          type: STRING
          advanced_data_type: null
          groupby: true
          filterable: true
          expression: null
          description: Error Message
          python_date_format: null
          extra: null
        - column_name: error_level
          verbose_name: Error Level
          is_dttm: false
          is_active: true
          type: STRING
          advanced_data_type: null
          groupby: true
          filterable: true
          expression: null
          description: Error Level
          python_date_format: null
          extra: null
        - column_name: ctx_dataset
          verbose_name: Context Dataset
          is_dttm: false
          is_active: true
          type: STRING
          advanced_data_type: null
          groupby: true
          filterable: true
          expression: null
          description: Context Dataset
          python_date_format: null
          extra: null
        - column_name: ctx_dataset_type
          verbose_name: Context Dataset Type
          is_dttm: false
          is_active: true
          type: STRING
          advanced_data_type: null
          groupby: true
          filterable: true
          expression: null
          description: Context Dataset Type
          python_date_format: null
          extra: null
        - column_name: extractor_status
          verbose_name: Extractor Status
          is_dttm: false
          is_active: true
          type: STRING
          advanced_data_type: null
          groupby: true
          filterable: true
          expression: null
          description: Extractor Status
          python_date_format: null
          extra: null
        - column_name: validator_status
          verbose_name: Validator Status
          is_dttm: false
          is_active: true
          type: STRING
          advanced_data_type: null
          groupby: true
          filterable: true
          expression: null
          description: Validator Status
          python_date_format: null
          extra: null
        - column_name: dedup_status
          verbose_name: Deduplication Status
          is_dttm: false
          is_active: true
          type: STRING
          advanced_data_type: null
          groupby: true
          filterable: true
          expression: null
          description: Deduplication Status
          python_date_format: null
          extra: null
        - column_name: denorm_status
          verbose_name: Denormalization Status
          is_dttm: false
          is_active: true
          type: STRING
          advanced_data_type: null
          groupby: true
          filterable: true
          expression: null
          description: Denormalization Status
          python_date_format: null
          extra: null
        - column_name: transformer_status
          verbose_name: Transformer Status
          is_dttm: false
          is_active: true
          type: STRING
          advanced_data_type: null
          groupby: true
          filterable: true
          expression: null
          description: Transformer Status
          python_date_format: null
          extra: null
        version: 1.0.0
        database_uuid: 04e7a613-525a-4ec7-80b6-e703584cc743
    - allow_csv_upload: false
      allow_ctas: false
      allow_cvas: false
      allow_run_async: false
      allow_dml: false
      expose_in_sqllab: true
      database_name: lakehouse
      sqlalchemy_uri: trino://trino@hudi-trino.trino.svc.cluster.local/lakehouse/hms
      uuid: a5abc507-1743-41ef-9fc7-256dfde8d497
      version: 1.0.0

extraSecrets:
  custom_sso_security_manager.py: |
    import logging
    import os
    import redis
    from superset.security import SupersetSecurityManager
    from flask_appbuilder.security.manager import AUTH_OAUTH

    def env(key, default=None):
        return os.getenv(key, default)

    def set_token_for_user(user_id, token, expiration=86400):
        sso_cache = redis.Redis(
            host=env("REDIS_HOST"),
            port=int(env("REDIS_PORT")),
            password=env("REDIS_PASSWORD"),
            db=int(env("REDIS_DB", 1)),
            decode_responses=False,
        )
        print(f"Setting token for user {user_id}: {token[:20]}... (expires in {expiration} seconds)")
        sso_cache.setex(f"sso_user_token:{user_id}", expiration, token)

    class CustomSsoSecurityManager(SupersetSecurityManager):
        def __init__(self, appbuilder):
            super(CustomSsoSecurityManager, self).__init__(appbuilder)
            app = self.appbuilder.get_app
            app.config.setdefault("AUTH_ROLES_MAPPING", {})
            app.config.setdefault("AUTH_TYPE", AUTH_OAUTH)

        def auth_user_oauth(self, userinfo):
            allowed_group = env("COGNITO_ALLOWED_GROUP", "")
            if allowed_group:
                user_groups = userinfo.get("role_keys", [])
                if allowed_group not in user_groups:
                    logging.warning(
                        "User '%s' blocked: not in allowed group '%s'. Groups: %s",
                        userinfo.get("username"), allowed_group, user_groups,
                    )
                    return None
            return super().auth_user_oauth(userinfo)

        def oauth_user_info(self, provider, response=None):
            logging.info("Oauth2 provider: %s", provider)

            if provider == "cognito":
                import base64, json as _json
                token_data = self.appbuilder.sm.oauth_remotes[provider].token
                access_token = token_data.get("access_token") if token_data else None
                id_token = token_data.get("id_token") if token_data else None

                # decode id_token to get cognito:groups (injected by pre-token Lambda)
                id_claims = {}
                if id_token:
                    try:
                        payload = id_token.split(".")[1]
                        payload += "=" * (4 - len(payload) % 4)
                        id_claims = _json.loads(base64.urlsafe_b64decode(payload))
                        logging.info("ID token claims: %s", id_claims)
                    except Exception as e:
                        logging.warning("Failed to decode id_token: %s", e)

                _region = env("COGNITO_REGION", "us-east-1")
                _domain = env("COGNITO_DOMAIN", "")
                _custom_domain = env("COGNITO_CUSTOM_DOMAIN", "")
                userinfo_url = (
                    f"https://{_custom_domain}/oauth2/userInfo"
                    if _custom_domain
                    else f"https://{_domain}.auth.{_region}.amazoncognito.com/oauth2/userInfo"
                )
                me = self.appbuilder.sm.oauth_remotes[provider].get(userinfo_url)
                me.raise_for_status()
                data = me.json()
                logging.info("User info from Cognito: %s", data)

                username = (
                    data.get("username")
                    or data.get("cognito:username")
                    or id_claims.get("cognito:username")
                    or data.get("sub", "")
                )
                first_name = data.get("given_name", "")
                last_name = data.get("family_name", "")
                if not first_name and not last_name:
                    full = data.get("name", username)
                    parts = full.split(" ", 1)
                    first_name = parts[0]
                    last_name = parts[1] if len(parts) > 1 else ""

                if access_token and username:
                    expires = token_data.get("expires_in", 86400)
                    set_token_for_user(username, access_token, expires)

                # get groups from id_token first, fallback to userinfo
                groups = id_claims.get("cognito:groups", data.get("cognito:groups", []))
                logging.info("User groups: %s", groups)
                return {
                    "name": data.get("name", f"{first_name} {last_name}".strip()),
                    "email": data.get("email", ""),
                    "first_name": first_name,
                    "last_name": last_name,
                    "id": username,
                    "username": username,
                    "role_keys": groups,
                }

            if provider == "obsrv":
                token_data = self.appbuilder.sm.oauth_remotes[provider].token
                access_token = token_data.get("access_token") if token_data else None

                me = self.appbuilder.sm.oauth_remotes[provider].get(
                    f'http{{ if .Values.global.ssl_enabled }}s{{ end }}://{{ .Values.global.domain }}/auth/realms/obsrv/protocol/openid-connect/userinfo'
                )
                me.raise_for_status()
                data = me.json()
                logging.info("User info from Keycloak: %s", data)

                if access_token and data.get("preferred_username"):
                    set_token_for_user(
                        data["preferred_username"],
                        token_data.get("access_token"),
                        token_data.get("expires_in", 86400),
                    )

                return {
                    "name": data.get("name", ""),
                    "email": data.get("email", ""),
                    "first_name": data.get("given_name", ""),
                    "last_name": data.get("family_name", ""),
                    "id": data.get("preferred_username", ""),
                    "username": data.get("preferred_username", ""),
                }

            return {}

# A dictionary of overrides to append at the end of superset_config.py - the name does not matter
# WARNING: the order is not guaranteed
configOverrides:
  enable_feature_flags: |
    FEATURE_FLAGS = {
      "DASHBOARD_RBAC": True,
      "DASHBOARD_NATIVE_FILTERS": True,
      "DASHBOARD_CROSS_FILTERS": True,
      "DASHBOARD_NATIVE_FILTERS_SET": True,
      "ENABLE_TEMPLATE_PROCESSING": True,
      "EMBEDDED_SUPERSET": True,
      "DISABLE_EMBEDDED_SUPERSET_LOGOUT": True,
    }

  data_cache_config: |
    DATA_CACHE_CONFIG = {
      'CACHE_TYPE': 'redis',
      'CACHE_DEFAULT_TIMEOUT': 600,
      'CACHE_KEY_PREFIX': 'superset_',
      'CACHE_REDIS_URL': 'redis://{{ tpl .Values.global.valkey_dedup.host . }}:{{ tpl (.Values.global.valkey_dedup.port | toString) . }}/{{ tpl (toString .Values.redis.db_index) $ }}'
    }

  sql_alchemy_config: |
    import os
    SQLALCHEMY_DATABASE_URI = f"postgresql://{os.getenv('DB_USER')}:{os.getenv('DB_PASS')}@{os.getenv('DB_HOST')}:{os.getenv('DB_PORT')}/{os.getenv('DB_NAME')}"
    SQLALCHEMY_TRACK_MODIFICATIONS = True
    SECRET_KEY = 'xCHEuo3xQNLdqgvM7GAR4Gf9kjyQzFzIrGi1maZdb0xibHu47o5hIyAX'

  # Guest Token configuration for embedded dashboard support
  # The GUEST_TOKEN_JWT_SECRET must match the secret used by the Lambda/backend generating guest tokens
  guest_token_config: |
    GUEST_TOKEN_JWT_SECRET = 'xCHEuo3xQNLdqgvM7GAR4Gf9kjyQzFzIrGi1maZdb0xibHu47o5hIyAX'
    GUEST_TOKEN_JWT_AUDIENCE = 'superset'
    GUEST_ROLE_NAME = 'Gamma'
    GUEST_TOKEN_JWT_EXP_SECONDS = 3600  # 1 hour - adjust based on your security requirements

  proxy_fix: |
    ENABLE_PROXY_FIX = True
    PUBLIC_ROLE_LIKE = "Admin"
  # map_box_key: |
  #  MAPBOX_API_KEY=''

  oauth: |
    import os
    from flask_appbuilder.security.manager import AUTH_OAUTH

    _cognito_region = os.getenv("COGNITO_REGION", "us-east-1")
    _cognito_domain = os.getenv("COGNITO_DOMAIN", "")
    _cognito_userpool_id = os.getenv("COGNITO_USERPOOL_ID", "")
    _cognito_custom_domain = os.getenv("COGNITO_CUSTOM_DOMAIN", "")
    _cognito_base = (
        f"https://{_cognito_custom_domain}"
        if _cognito_custom_domain
        else f"https://{_cognito_domain}.auth.{_cognito_region}.amazoncognito.com"
    )

    AUTH_TYPE = AUTH_OAUTH

    OAUTH_PROVIDERS = [
      {
        "name": "cognito",
        "icon": "fa-amazon",
        "token_key": "access_token",
        "remote_app": {
          "client_id": os.getenv("COGNITO_CLIENT_ID"),
          "client_secret": os.getenv("COGNITO_CLIENT_SECRET"),
          "api_base_url": f"{_cognito_base}/",
          "access_token_url": f"{_cognito_base}/oauth2/token",
          "authorize_url": f"{_cognito_base}/oauth2/authorize",
          "jwks_uri": (
            f"https://cognito-idp.{_cognito_region}.amazonaws.com"
            f"/{_cognito_userpool_id}/.well-known/jwks.json"
          ),
          "server_metadata_url": (
            f"https://cognito-idp.{_cognito_region}.amazonaws.com"
            f"/{_cognito_userpool_id}/.well-known/openid-configuration"
          ),
          "client_kwargs": {
            "scope": "openid email profile",
            "token_endpoint_auth_method": "client_secret_post",
          },
          "redirect_uri": os.getenv("COGNITO_REDIRECT_URL"),
          "params": {
            "identity_provider": "CognizantSSO",
          },
        },
      }
    ]

    from custom_sso_security_manager import CustomSsoSecurityManager
    CUSTOM_SECURITY_MANAGER = CustomSsoSecurityManager
    AUTH_USER_REGISTRATION = True
    AUTH_USER_REGISTRATION_ROLE = os.getenv("COGNITO_DEFAULT_ROLE", "Gamma")
    AUTH_ROLES_MAPPING = {
        "us-east-1_QuCefvh0J_superset-users": ["Admin"],
    }
    AUTH_ROLES_SYNC_AT_LOGIN = True

configMountPath: "/app/pythonpath"
extraConfigMountPath: "/app/configs"
dashboardMountPath: "/app/dashboards"

image: &image
  registry: docker.io
  repository: apache/superset
  tag: 3.0.2
  pullPolicy: IfNotPresent

<<: *image

imagePullSecrets: []

service:
  type: ClusterIP
  port: 8088
  annotations: {}

loadBalancerService:
  enabled: true
  type: LoadBalancer
  externalTrafficPolicy: Local
  certificateArn: ""
  annotations:
    service.beta.kubernetes.io/aws-load-balancer-type: nlb
    service.beta.kubernetes.io/aws-load-balancer-scheme: internet-facing
  ports:
    - name: http
      port: 80
      targetPort: 8088
    - name: https
      port: 443
      targetPort: 8088

ingress:
  enabled: false
  annotations: {}
    # kubernetes.io/ingress.class: nginx
    # kubernetes.io/tls-acme: "true"
    ## Extend timeout to allow long running queries.
    # nginx.ingress.kubernetes.io/proxy-connect-timeout: "300"
    # nginx.ingress.kubernetes.io/proxy-read-timeout: "300"
    # nginx.ingress.kubernetes.io/proxy-send-timeout: "300"
  path: /
  pathType: ImplementationSpecific
  hosts:
    - chart-example.local
  tls: []
  #  - secretName: chart-example-tls
  #    hosts:
  #      - chart-example.local

resources:
  # We usually recommend not to specify default resources and to leave this as a conscious
  # choice for the user. This also increases chances charts run on environments with little
  # resources, such as Minikube. If you do want to specify resources, uncomment the following
  # lines, adjust them as necessary, and remove the curly braces after 'resources:'.
  limits:
    cpu: 512m
    memory: 1024Mi
  requests:
    cpu: 250m
    memory: 512Mi

##
## Superset node configuration
supersetNode:
  connections:
    redis_host: "{{ .Values.global.valkey_dedup.host }}"
    redis_port: "{{ .Values.global.valkey_dedup.port }}"
    db_host: "{{ .Values.global.postgresql.host }}"
    db_port: "{{ .Values.global.postgresql.port }}"
    db_user: "{{ .Values.global.postgresql.superset.user }}"
    db_pass: "{{ .Values.global.postgresql.superset.password }}"
    db_name: "{{ .Values.global.postgresql.superset.name }}"
  forceReload: false # If true, forces deployment to reload on each upgrade
  # initContainers:
  #   - name: wait-for-postgres
  #     image: busybox:latest
  #     imagePullPolicy: IfNotPresent
  #     envFrom:
  #       - secretRef:
  #           name: '{{ tpl .Values.envFromSecret . }}'
  #     command: [ "/bin/sh", "-c", "until nc -zv $DB_HOST $DB_PORT -w1; do echo 'waiting for db'; sleep 1; done" ]
  ## Annotations to be added to supersetNode deployment
  deploymentAnnotations: {}
  ## Annotations to be added to supersetNode pods
  podAnnotations: {}

##
## Init job configuration
init:
  # Configure resources
  # Warning: fab command consumes a lot of ram and can
  # cause the process to be killed due to OOM if it exceeds limit
  resources: {}
    # limits:
    #   cpu:
    #   memory:
    # requests:
    #   cpu:
    #   memory:
  command:
    - "/bin/sh"
    - "-c"
    - ". {{ .Values.configMountPath }}/superset_bootstrap.sh; . {{ .Values.configMountPath }}/superset_init.sh"
  enabled: true
  createAdmin: true
  # initContainers:
  #   - name: wait-for-postgres
  #     image: busybox:latest
  #     imagePullPolicy: IfNotPresent
  #     envFrom:
  #       - secretRef:
  #           name: '{{ tpl .Values.envFromSecret . }}'
  #     command: [ "/bin/sh", "-c", "until nc -zv $DB_HOST $DB_PORT -w1; do echo 'waiting for db'; sleep 1; done" ]
  initscript: |-
    #!/bin/sh
    echo "Upgrading DB schema..."
    superset db upgrade
    echo "Initializing roles..."
    superset init
    {{ if .Values.init.createAdmin }}
    echo "Creating admin user..."
    superset fab create-admin \
                    --username {{ .Values.adminUser.username }} \
                    --firstname {{ .Values.adminUser.firstname }} \
                    --lastname {{ .Values.adminUser.lastname }} \
                    --email {{ .Values.adminUser.email }} \
                    --password {{ .Values.adminUser.password }} \
                    || true
    {{ end }}
    if [ -f "{{ .Values.extraConfigMountPath }}/import_datasources.yaml" ]; then
      echo "Importing database connections.... "
      superset import_datasources -p {{ .Values.extraConfigMountPath }}/import_datasources.yaml
    fi

    if [ -f "{{ .Values.dashboardMountPath }}/dashboards.zip" ]; then
      echo "Importing dashboards.... "
      superset import-dashboards --path "{{ .Values.dashboardMountPath }}/dashboards.zip"
    else
      echo "File /app/dashboards/dashboards.zip not found..."
    fi

nodeSelector: {}

tolerations: []

affinity: {}
